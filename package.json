{
  "name": "workspace",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "cd react-aria-carousel && bun run build && cd ../site && bun panda && bun run build",
    "prepare": "bun --cwd react-aria-carousel panda codegen && bun --cwd site panda codegen "
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.2.1",
    "prettier": "^3.2.5"
  },
  "workspaces": [
    "react-aria-carousel",
    "site"
  ],
  "trustedDependencies": [
    "@pandadev/css",
    "example",
    "react-aria-carousel"
  ]
}
