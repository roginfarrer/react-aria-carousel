{
  "name": "workspace",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "cd react-aria-carousel && bun run build && cd ../site && bun run build"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.2.1",
    "prettier": "^3.2.5"
  },
  "workspaces": [
    "react-aria-carousel",
    "site"
  ]
}
